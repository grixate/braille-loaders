{
  "name": "@dot-loaders/presets",
  "version": "0.1.0",
  "description": "Curated preset library of braille-dot loading animations for @dot-loaders/core.",
  "keywords": ["loader", "spinner", "braille", "unicode", "presets"],
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/grixate/dot-loaders.git",
    "directory": "packages/presets"
  },
  "homepage": "https://grixate.github.io/dot-loaders/",
  "bugs": "https://github.com/grixate/dot-loaders/issues",
  "type": "module",
  "main": "./dist/index.cjs",
  "module": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js",
      "require": "./dist/index.cjs"
    }
  },
  "files": ["dist"],
  "publishConfig": {
    "access": "public"
  },
  "dependencies": {
    "@dot-loaders/core": "workspace:*"
  },
  "scripts": {
    "build": "tsup src/index.ts --format esm,cjs && tsc -p tsconfig.json --emitDeclarationOnly --declaration --declarationMap false --outDir dist",
    "lint": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  }
}
